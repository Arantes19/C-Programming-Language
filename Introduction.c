#include <stdio.h> //Preprocessor command that tells to compiler to include the contents of a file 

int main(){

    printf("I like Pizza!\n"); //Displays my output, need to have quots 
    printf("It's really good!\n");
    return 0; //This returns the exit status of our program, no errors (0), with errors (1)
}